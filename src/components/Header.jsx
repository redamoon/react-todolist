import React from 'react';

const Header = (props) => (
  <header>
    <h1 className="siimple-box-title siimple--color-white">React Todo App (Reactもくもく会 Sapporo)</h1>
  </header>
);

export default Header;
